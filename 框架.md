# 1. RT-Thread框架
1. rt_device_find                找到设备
2. rt_device_open              给IP上电
3. rt_device_control           对IP进行改频率、启动、停止操作
4. rt_device_write               给IP寄存器写值
5. rt_device_close              给IP下电

# 2. BSP
BSP = Board(板级逻辑) + Driver(设备驱动)
- 里面全是 `readl()`，`writel()` 和寄存器地址？ → **Driver**
- 里面全是 `#define LED_PIN` 和 GPIO 编号？ → **Board**
driver提供函数接口供board调用

SDK (完整工作间)
 └── BSP (工作台)
      ├── Driver (工具)
      └── Board (图纸)

# 3. 代码框架
## 3.1 RTOS
**T2605项目M85 RTOS更新为5.2.2版本，同时a500项目文件夹改名为imv；（T2515项目M7 RTOS版本是旧版本，a400项目文件夹名为tetra）**
`SConscript` 的作用就是：各子目录用于定义自身模块的“源文件清单、头文件路径及编译条件”，并将其注册汇报给主构建系统。
编译开关:
im/rtos/rtos/rt-thread/rt-thread/bsp/tetras/a500_soc/a500_fpga/.config
testcase路径：
im/rtos/testing/tests
~~项目宏开关：~~
~~im/boards/a500_fpga/board/uboot/a500_fpga_defconfig~~
测试用例的shell cmd:
im/rtos/testing/tests/test_cfg/configs/a400_desc.json
中断注册：
im/rtos/rtos/rt-thread/rt-thread-5.2.2/bsp/asm9260t/platform/interrupt.c
### 3.1.1 timer
编译开关：TEST_HWTIMER
测试代码：im/rtos/testing/tests/peri/hwtimer
设备树：im/rtos/rtos/rt-thread/rt-thread/bsp/tetras/a500_soc/common/device_config_def.c

### 3.1.2 menuconfig
menuconfig: im\rtos\rtos\rt-thread\rt-thread-5.2.2\bsp\imv\a500_soc\a500_fpga下执行scons --menuconfig
注：menuconfig fail执行pip3 install kconfiglib --user -i http://mirrors.imv.local/repository/pypi/simple --trusted-host mirrors.imv.local


# 4 Cortex-M
## 4.1 基本概念
硬件架构：cortex-m(M核)
嵌入式操作系统：RT-Thread（属于RTOS）
经典三层结构：
————————————————————
应用层/ 测试用例（GTest）               最上层
————————————————————
RTOS操作系统层（RT-Thread）        中间层：管理任务，提供API
————————————————————
硬件架构层（M核，NVIC，寄存器）  最底层：物理芯片和它的寄存器
————————————————————
## 4.2 中断流程
1. 外设：外设检测到触发条件产生中断[IP中断pending]，可通过外设IP的中断状态寄存器查询，外设会通过芯片内部的物理引脚（IRQ Line），
向NVIC（中断控制器）发送高电平或脉冲信号，[到其他核也是类似原理，肯定有暗线连到其他核]
2. NVIC：NVIC接收到外设传过来的中断，会在NVIC_ISPR中将对应的中断挂起（pending=1），需要保证通道开启，不然中断会被NVIC拒之门外，
通道开启通过函数NVIC_EnableIRQ，通道开通后，且当前没有更高优先级，会向CPU发起中断请求
3. CPU：硬件响应，CPU 检查全局中断开关（PRIMASK，cpsie i汇编指令）。如果开着，则自动压栈，并根据中断号去查中断向量表（Vector Table）
 找对应中断入口地址并强行跳转
4. RT-Thread：通过汇编保存剩余现场，查阅软件中断注册表，找到当前中断对应的C语言函数指针
5. 业务：执行中断回调函数

IRQ与Vector偏差
物理外设中断号（IRQ Number）:芯片设计给中断的编号，NVIC寄存器控制的都是这个物理号
内核异常向量号（Vector Number）：M核给自己预留16个系统异常，因此外设排队必须从16开始，绝对向量号=IRQ Number + 16
[操作 NVIC 寄存器时用 197；操作 RT-Thread 注册函数 rt_hw_interrupt_install 时用 213。]

问题排查：
1. 查外设：
读外设的STATUS寄存器，如果是0，说明外设中断没触发
2. 查 NVIC 挂起（ISPR）：
如果是 0，说明外设到 CPU 的物理连线断了（FPGA 常见 Bug）。
如果是 1，说明通路是好的，信号到了 CPU 门口。

int irq = 197; // 你的中断号
// 1. 计算该中断号落在 NVIC_ISPR 的哪一个 32 位寄存器上 [0xE000E200是M核固定地址]
uint32_t ispr_addr = 0xE000E200 + (irq / 32) * 4;
// 2. 计算在该寄存器中的哪一位 (0 ~ 31)
uint32_t bit_offset = irq % 32;
// 3. 读取该地址的值，并判断该位是否为 1
uint32_t ispr_val = (volatile uint32_t * )ispr_addr;
uint32_t is_pending = (ispr_val >> bit_offset) & 0x1;
LOG_I("IRQ %d NVIC ISPR Addr: 0x%x, Val: 0x%x, Is_Pending: %d\n", irq, ispr_addr, ispr_val, is_pending);   

3. 查 NVIC 使能（ISER）与全局中断（PRIMASK）：
如果 ISER 是 0，说明没调用 EnableIRQ。
如果使能开了，还不响应，说明全局中断被锁死了。

int irq = 197; // 你的中断号
// [0xE000E100 是M核固定地址]
uint32_t iser_addr = 0xE000E100 + (irq / 32) * 4;
uint32_t iser_val =  *(volatile uint32_t *)iser_addr;
uint32_t is_enabled = (iser_val >> (irq % 32)) & 0x1;
LOG_I("IRQ %d NVIC ISER Addr: 0x%x, Val: 0x%x, Is_Enabled: %d\n", irq, iser_addr, iser_val, is_enabled);

若是NVIC通道没开，手动开一下
// 1. 【解除 NVIC 屏蔽】强行向 ISER 寄存器写入对应位，使能该中断
int32_t iser_addr = 0xE000E100 + (irq / 32) * 4;
*(volatile uint32_t *)iser_addr= (1 << (irq % 32));

// 2. 【双重保险】强开 CPU 全局中断（防止测试框架关了全局中断）
__asm volatile ("cpsie i" : : : "memory");
![[file-20260716170646651.png]]

4. 查注册表：
如果报 handler is null，百分之百是注册的中断号（比如 197）与内核实际跳转的中断号（213）产生了偏差。
 ret = rt_hw_interrupt_install(AON_SS_SYSCNT_TOP_INT, syscnt_isr, NULL, "syscnt");




