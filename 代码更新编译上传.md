# 1 拉取仓
1，定位代码仓分支资源  
manifest=default.xml 
repo init -u ssh://celestechen@gerrit.imv.local:29418/manifests -b master -m default.xml --repo-url=ssh://celestechen@gerrit.imv.local:29418/git-repo --repo-branch=stable --no-repo-verify
2，同步下拉代码  
repo sync -j64  
3, 设置钩子  
repo forall -c "scp -p -P 29418 celestechen@gerrit.imv.local:hooks/commit-msg ".git/hooks/""

 repo forall -c '                                                                                                                                          
  HOOK_DIR="$(git rev-parse --git-dir)/hooks" && \
  mkdir -p "$HOOK_DIR" && \
  curl -RLo "$HOOK_DIR/commit-msg" https://gerrit.imv.local/tools/hooks/commit-msg && \
  chmod +x "$HOOK_DIR/commit-msg" || echo "❌ 仓库 $(pwd) 安装 commit-msg 钩子失败"
'
4, 切换到manifest默认的开发分支  
repo start --default-name --all

# 2 编译
source build/env/im_sdk_env_setup.sh
cd boards/a500_fpga/ 
make pre_make  
make a500_fpga_config

全部编译：   make -j32
M85/M7：   make rtos -j8
dsp：           make dsp -j32
SV：            make sv_gtest -j32
M3:              make build_boot -j64，结果在images/preboot  [M3起来后，ctrl+e进入串口，ctrl+d退出]
spl：            make spl_menuconfig(选LPDDR4/5配置） ||   make spl_savedefconfig
atool:           make atool -j8      im\out\a500_fpga\system\bin
          make build_system -j8          im/out/a500_fpga/images/system.img
make clean后要编译make pre_make、make a400_udp_config才能正常编  

# 3 入库
1. git status    查看当前修改
2. git remote -v 进入当前文件所属的目录查看属于哪个仓
3. git fetch origin  获取远端信息
4. git log --oneline HEAD..origin/master -- xx.c    查看修改信息
5. git diff HEAD..origin/master -- xx.c   查看远端修改了什么
6.  1）git add .   添加所有修改文件
    2）git add build/cfg/dsp.cfg  添加具体的文件
7. git commit -s  提交上传信息（有格式要求）

8. git push origin HEAD:refs/for/master   push到gerrit
git config --local --unset-all user.name
git config --local --unset-all user.email

git --no-pager config user.name
git --no-pager config user.email

git commit --amend --reset-author --no-edit --signoff

6. set topic

git commit --amend  可重新修改上传信息

git add非必要文件后，可通过git reset撤回
![[file-20260729170056543.png]]



![[file-20260730105728209.png]]


 第1步：先把你自己的改动提交到本地（不推送到远程）
git add tetras_syscnt.c
git commit -m "描述你的改动"

第2步：拉取远程最新代码（会尝试自动合并）
git pull origin master

第3步：如果有冲突，Git会提示你，解决后再次 commit；如果没有冲突，直接推送
git push origin master








# 4 patch
代码流程：提交--code review--merge
在其他人提交代码但未merge时，可采用打patch方式更新代码
1. gerrit上确认需要合并的代码
2. 获取patch 链接
![[file-20260629195118915.png]]
3. 将服务器目录打开到与patch同一个目录下，输入链接
![[file-20260630101202864.png]]
注意：如果本地也有改动，则会合并冲突
      有多个合并项时，从时间早到时间晚的顺序合入
  合并冲突时解决方法：
  1 git stash -u，保存本地版本
  ![[file-20260630103246976.png]]
  2  输入合并链接
  ![[file-20260630104647238.png]]
  3 git stash pop   合入本地修改，但也有报错可能，确定是哪个文件
  ![[file-20260630104748522.png]]
  4 git stash show -p  查看文件区别
  5  git checkout stash@{0} -- a500_soc/a500_fpga/.config   强制合入
  6  git stash drop stash@{0}  释放本地存储
  7 git stash list 查看存储



NEW: M85: Support m85 boot use atool

WHY: as title

JIRA: #[T2605BU-320](http://bu-jira.imv.local/browse/T2605BU-320)
TEST: Pass

Change-Id: Ibf738bfaae10bdad7a068ba5e3c0a7b0e0367003
Signed-off-by: gang.du <jonesdu@ai-imvision.cn>
Reviewed-on: [https://gerrit.imv.local/c/tools/atool/+/81319](https://gerrit.imv.local/c/tools/atool/+/81319)
Reviewed-by: Dave Yi <daveyi@ai-imvision.cn>
Tested-by: imv-ci <imv.ci@imvision.cn>


